apiVersion: v1
kind: Service
metadata:
  name: osac-ui
  namespace: innabox-devel
  labels:
    app: osac-ui
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: osac-ui
